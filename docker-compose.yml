version: '3.8'

services:
  arun-ai:
    build: .
    container_name: arun-ai-saas
    restart: always
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./data:/app/data